{
  "name": "tackle-tracker",
  "version": "1.0.0",
  "description": "Personal fishing gear inventory management app",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"server,client\" -c \"cyan,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
